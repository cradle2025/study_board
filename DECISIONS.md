# DECISIONS.md — 决策记录

> **只追加，永不修改历史条目。**
> 发现旧条目写错了，追加一条新的说明更正，不要编辑旧的 ——
> 这个文件的价值在于「当时的判断依据」，改掉它就失去了这个价值。
>
> 条目格式：
> - **做了什么**
> - **为什么这么选**
> - **否掉了哪些方案（附理由）**
> - **结果如何（含验证方式）**
> - **下一步依赖什么**

---

## D-001 · 数据格式用「版本闸口」而不是靠各存储自己的 version 字段

**做了什么**
新增 `src/main/services/dataVersion.ts`，数据根目录放 `data-version.json`
记录 `schema` 与写入它的 app 版本。启动时（建存储之前）过闸口，四条分支：
`fresh` 写印记 / `same` 只刷新 app 版本 / `upgrade` 先整目录备份再逐级迁移 /
`downgrade`（数据比程序新）备份 + 显著提示**但继续启动**。

**为什么这么选**
各存储原来自己的 `version` 字段**只写不读**，等于注释 —— 没有任何代码
依据它做判断。数据格式变更时没有任何保护。

降级**不阻止启动**是有意的：直接拒绝启动对学习工具太重（用户打不开自己的
东西），静默继续又等于默认同意丢数据。折中是备份 + 提示。

**否掉了哪些方案**
- 「各存储自己管迁移」：迁移需要跨存储的一致性（比如课表和卡片同时变），
  分散管理会出现「一半迁完一半没迁」的状态。
- 「降级时拒绝启动」：太粗暴，且用户没有自救路径。

**结果如何**
`npm run smoke:update`（降级保护）与 `npm run smoke:migrate`（老数据升级）通过。
**验证方式**：两个场景对印记的期望正好相反（降级要保留、升级要改写），
所以必须分开断言 —— 第一版把两者并进一条共用函数，升级场景被误判失败。

**下一步依赖什么**
将来每次改数据格式，都要递增 `DATA_SCHEMA` + 注册迁移函数 + 加自检场景。

---

## D-002 · 便携模式改成「真的搬数据」

**做了什么**
新增 `settings:set-portable` 通道，真的复制数据、写 `portable.flag`、重启生效。
`portableMode` 从 `SettingsPatch` 移除，`sanitizePatch` 遇到它直接报错。

**为什么这么选**
原来开关只写了 `config.json` 的一个字段，而数据读哪里由
`detectPortableMode()`（看程序同级有没有 `portable.flag`）决定 ——
`writePortableFlag` 和 `migrateTo` 是**没有任何调用点的死代码**。
开关看着生效了（勾变了、还提示「重启后生效」），数据位置从来没动过。

**否掉了哪些方案**
- 「静默忽略未知字段」：**静默忽略正是原 bug 的形态**。
  宁可让 IPC 报错，也不要再出现「看起来生效了其实没动」。

**结果如何**
开关真的搬数据。**验证方式**：设置页操作后检查目标目录有文件、
`portable.flag` 存在、重启后应用读的是新位置。

**下一步依赖什么**
无。

---

## D-003 · 升级时用 NSIS 钩子保住便携数据（附一个跨版本契约）

**做了什么**
`build/installer.nsh` 里用 `customUnInstall` / `customInstall`，
在 `${isUpdated}` 时把 `study-board-data` 挪到 `$TEMP` 再挪回来。

**为什么这么选**
便携数据目录就在安装目录里，而 NSIS 卸载器会把 `$INSTDIR` 整个删掉
（重命名到 `$PLUGINSDIR\old-install` 再 `RMDir /r`，那是个会被清理的临时目录）。
升级时也会先跑旧版卸载器，所以升级同样会删。

**没有**接管默认删除逻辑（用 `customRemoveFiles`），因为模板里的
`un.atomicRMDir` 处理了「文件被占用」的情况，接管会丢掉那个能力。

**否掉了哪些方案**
- 「把便携数据放到安装目录之外」：便携模式的意义就是数据跟着 U 盘走，
  放到 `%LOCALAPPDATA%` 就失去意义了。

**结果如何**
实机验证：装 0.3.0 → 植入 `study-board-data`（含一篇内容可辨认的笔记）→
用同一个包覆盖安装 → **数据完好无损**，`$TEMP` 无暂存残留。

**⚠️ 一个必须记住的约束（实测得出，不是推断）**
装 0.1.0（无保护）→ 植入数据 → 装 0.3.0（有保护）→ **数据被删了**。
因为**升级时跑的是「已安装版本」的卸载器**，不是新版编译出来的那个。
所以这套保护只在「被升级的版本自己也带这段逻辑」时生效。

对初版无影响（0.1.0 / 0.2.0 从未对外发布），但：

**`SB_STASH_DIR` 是跨版本契约，不能改。**
升级时「暂存到哪」由旧版卸载器决定，「从哪恢复」由新版安装程序决定 ——
两边是两个不同版本编译出来的代码。改这个路径会让从旧版升级的数据
留在 `$TEMP` 而新版找不到，表现就是「升级后数据不见了」。

**下一步依赖什么**
任何改 `installer.nsh` 的人都要先读文件顶部的注释。

---

## D-004 · i18n 用扁平点分 key + 两层降级，词典放 `shared`

**做了什么**
`src/shared/i18n/` 放 `zh-CN.ts` / `en-US.ts` / `index.ts`，
`translate(lang, key, params)`；渲染层包一层 `t()` / `tm()`。

**为什么这么选**
- **放 `shared` 而不是渲染层**：主进程抛的报错也会出现在界面上
  （IPC 错误经 `formatError` 变成 toast），两边必须用同一份词典。
  放在任一侧都会让另一侧只能硬编码。
- **扁平点分 key 而不是嵌套对象**：`t('notes.group.rename')` 这种写法让
  「漏翻译」可以被机器查出来 —— 嵌套对象要递归比对，扁平表一句
  `Object.keys` 就能比出差异。自检里那条「中英词条一一对应」的断言
  就是靠这个成立的。
- **两层降级**：英文缺 → 回落中文（用户至少看得懂）；两边都缺 →
  显示 key 本身。后者看着丑，但那正是它的意义：界面上出现
  `notes.foo.bar` 比出现空字符串好排查得多。也正因为这个降级，
  翻译可以分批做，中途不会把界面弄坏。

**否掉了哪些方案**
- 「用成熟的 i18n 库（i18next 等）」：需求只有「两语言 + 参数插值 +
  缺翻译降级」，引入库带来的体积与配置复杂度不划算，且库的
  「缺翻译回落」行为不容易被自检断言盯住。

**结果如何**
602 词条，中英一一对应。**验证方式**：
`npm run smoke`（含 `dictionaryOk()` 断言）+ `npm run smoke:i18n`。
渲染层只剩 2 处 `console.*` 中文。

**下一步依赖什么**
加词条必须两边一起加；改完跑 `smoke:i18n`。

---

## D-005 · 主进程的中文报错不逐个改造，改在「展示那一刻」查表

**做了什么**
主进程继续 `throw new Error('中文')`；渲染层展示前用 `tm()` 查表翻译，
查不到就原样显示中文。

**为什么这么选**
主进程的中文报错散在几十个服务里，还有很多是**动态拼**的
（如 `资料最多 200 份`）。逐个改造成 key 的成本极高，而且改错就丢错误信息
（报错本身是调试线索）。在展示层查表是成本最低、风险最小的位置。

**否掉了哪些方案**
- 「主进程改成抛错误码，渲染层拼文案」：动几十个文件，
  且拼装过程本身会引入新的漏翻点。
- 「主进程也 import 词典并翻译」：主进程不知道渲染层当前的语言
  （语言是渲染层的模块级状态），要传语言进来又是一圈改动。

**结果如何**
英文界面下大部分主进程报错会显示英文，少数动态拼的仍显示中文
（有意的降级）。**验证方式**：`smoke:i18n` 的逐页截图里没有出现
突兀的中文报错。

**下一步依赖什么**
新增主进程报错时，如果是**固定文案**，在 `en-US.ts` 里加同 key 即可；
如果是动态拼的，暂时接受显示中文。

---

## D-006 · 切换语言用「重绘」而不是「逐个更新文案」

**做了什么**
语言是模块级状态；`AppShell.#relocalize()` 重建外壳 DOM、重挂导航、
再 `go()` 回当前路由。

**为什么这么选**
文案散布在几十个视图与组件的构造函数里，还有一部分是**拼在 HTML 字符串里**
的。逐处更新等于给每个视图再写一遍渲染逻辑，漏一处就是一处永远不变的中文。
重建只有一处代价（当前视图的临时状态会丢），而切语言本来就是低频动作。

**踩到的坑（重要）**
`go()` 里有一句「路由没变就直接返回」，所以重绘前必须把 `#currentRoute`
清掉 —— 不清的话重挂载会静默什么都不做，表现是「切了语言但界面纹丝不动」，
而且**不报任何错**。

**否掉了哪些方案**
- 「逐层传 `lang` 参数」：会污染几乎每个函数签名，收益只是
  「同一时刻能存在两种语言的界面」——这个应用不需要那个能力。

**结果如何**
`npm run smoke` 的 `languageSwitchOk: true`（看板 → Dashboard → 看板，**双向**都验）。

**下一步依赖什么**
新增视图不需要各自订阅语言变化 —— 外壳重建会带上它们。
但如果某个视图在**模块加载期**就把文案固化进常量（比如 `Record<X, string>` 存文案），
那它永远变不了。这类表必须存 key。

---

## D-007 · 课表「连堂复用」：做两条路径，不做整列复制

**做了什么**
1. 单元格编辑器加「复制上一节」按钮（同一天上一节，**整格复制**）
2. 加「复用已录入的课程」下拉 + 课程名输入框的原生 `list` autocomplete
   （只填课程身份：名称/老师/地点/时长，**不填备注**）

**为什么这么选**
用户的原话是「很多课程是连堂上的」—— 连堂在表格里就是上下相邻的格子。
「复制上一节」一下点完，是最直接的解。
下拉覆盖非相邻的情况（周三的课和周五的课是同一门）。
不填备注是因为备注往往是**这一格特有的**（「小测」「调课」），
自动填进去反而要用户删。

**否掉了哪些方案**
- **「整列复制」**（把周一整列复制到周三）：**用户明确否决，理由是不值得**。
  我评估后同意：它有个确认框，说明是危险操作，而如果一周七天本来就不一样，
  这按钮根本用不上。技术上它也缺前提（见下）。

**结果如何**
`npm run smoke:timetable` 的 `reuseOk: true`、`reuseOptionCount: 5`、
`nameAfterCopy: "编译原理"`。

**下一步依赖什么**
无。但**顺带查清了一件事**：面板只有 `onSaveCell(key, cell)`，
而 `TimetableStore` 每个单格方法都调一次 `#persist()`（整份 JSON 重写）。
所以任何**多格操作**都必须先加批量接口（`setCells`），
否则 = N 次整文件重写 + 中途失败留下半成品。这个结论在周次功能里用得上。

---

## D-008 · 周次功能选「单元格级周次」，不做「多套课表」

**做了什么**
（决策已定，实现待做 —— 见 `HANDOFF.md` 第 8 节）

`TimetableCell` 加 `id` + `weeks: WeekRule`；
`cells` 从 `Record<key, TimetableCell>` 变成 `Record<key, TimetableCell[]>`。

**为什么这么选**
用户明确选了三个方案里最灵活的那个。而且它**最贴近真实教务系统**：
真实的课表就是「某个时间段、某些周、上某门课」，同一时间段不同周可以
是不同的课。用「多套课表」表达这个需要把同一门课在几套表里重复录入。

**否掉了哪些方案**
- **「单双周」**（只分两套）：最省事，但用户说「每一周都可能不一样」，
  单双周表达不了「前八周一门、后八周另一门」。
- **「多套课表按周次绑定」**（A/B/C 表各绑周次范围）：比单双周灵活，
  但同一门课要在多套表里重复录；且「第 3 周该显示哪套」这个映射本身
  就是个额外概念，用户要学。

**结果如何**
未实现，无法验证。

**下一步依赖什么**
⚠️ **数据层与 UI 层必须同一次落地。** 数据层先支持「一格多门课」、
UI 还按「一格一门课」读写的话，用户双击编辑再保存会**静默删掉**
这一格里其它周次的课。所以没有安全的中间点，不能分阶段。

---

## D-009 · 发布走 GitHub Releases，不做代码签名（用户决策）

**做了什么**
`.github/workflows/release.yml`：推 `v*` tag 触发，构建 Windows 安装包 +
`.blockmap` + `SHA256SUMS.txt`，先出 **draft** Release。

**为什么这么选**
用户明确说「走 github 发布，不走软件商店之类的」。
这个前提下，**签名从「必需品」降级成「可选项」**：
- 发布机制本身不需要签名（GitHub Releases 就是文件下载）
- 但 **SmartScreen 照样会拦** —— 它看的是「有没有可信签名 + 下载量够不够」，
  跟从哪下载无关。用户第一次运行仍会看到「已保护你的电脑」

**否掉了哪些方案**
- **买 OV / EV 证书**（约 $200 / $400 每年）：用户尚未决定。
  不签也能发，只是每个用户过一次拦截框。已把「不签名」这件事写进
  README 与 Release 正文，免得用户看到拦截框以为中招了。

**顺带查证（省得以后再翻）**
`electron-builder` 的 GitHub provider **不会**从 `GITHUB_REPOSITORY`
环境变量推断仓库 —— 它读的是 `package.json` 的 `repository` 字段
（`app-builder-lib/out/publish/PublishManager.js` 的 `packager.info.repositoryInfo`）。
但现有工作流用的是 `--publish never` + `softprops/action-gh-release` 挂附件，
**两条都不需要**，所以不受影响。

**结果如何**
工作流已就位，**从未在真实 Actions 上跑过**（仓库还没建）。

**下一步依赖什么**
1. 用户建 GitHub 仓库（指导已发出）
2. 确认仓库的 GitHub 用户名（登录名，ASCII）
3. 之后才能补 `repository` / `homepage` / `bugs` 字段

---

## D-010 · 周次功能：数据与 UI 同批落地，迁移自检放进 timetable 场景

**做了什么**
把 D-008 定下的「单元格级周次」实现了出来，在分支 `feat/week-rules` 上，
`master` 保持 0.3.0 不动。

- `TimetableCell` 加 `id` + `weeks: WeekRule`；
  `cells` 从 `Record<key, TimetableCell>` 变成 `Record<key, TimetableCell[]>`
- `DATA_SCHEMA` 1 → 2，注册**幂等**的 v1 → v2 迁移
- 存储改成按 id 增删改（`setCell` / `removeCell`）+ 批量 `setCells`
- 渲染层：一格堆叠多门课、周次角标、按 `currentWeek` 过滤；
  编辑器分「列表 / 表单」两形态
- 版本 0.3.0 → 0.4.0

**为什么这么选**
- **数据与 UI 同批**：D-008 已经论证过没有安全的中间点。落实方式是
  **第一个提交就同时包含数据层、迁移、IPC、渲染层、i18n**，
  不做「先数据后 UI」的分阶段提交。自检里专门有一条断言
  （编辑第一门课后第二门必须还在）盯着这个风险。
- **迁移自检放进 `smoke:timetable` 的数据准备**：迁移断言需要
  「闸口跑之前就有一份 v1 数据」，而 `prepareUpdateScenarioIfRequested()`
  是唯一能在闸口之前写文件的入口。让 timetable 场景也走这个入口之后，
  它**每次都真的走一遍备份 + 迁移**，界面断言全部落在迁移后的数据上。
  代价是 `filledOk` / `filledAfterEditOk` 的期望值从 3 / 4 变成 4 / 5。
- **迁移函数 `export` 出来只为自检**：幂等性没法从外部观察（第二次跑完
  形状应该「不变」），必须能再调一次才能断言。
- **「复制上一节」在列表形态下改成批量写入**：表单形态仍是预填单个课程
  （保持 D-007 的行为），列表形态把上一节的**所有课**一次复制过来，
  走 `setCells`。复制过来的课清空 id —— 沿用来源 id 会让同格出现两个
  同 id 的课，之后按 id 删除会打到错误的那一门上。

**否掉了哪些方案**
- **「只验备份目录存在」**：把 `makeBackup` 挪到迁移之后，这种断言照样全绿，
  而备份里已经是新格式、等于没有回头路。改成断言**备份里那份还是 v1 的
  单对象形状** —— 形状本身就是「拍在什么时候」的判据。
- **读磁盘时给缺 id 的格子补一个 id**：宽容，但 id 是「按 id 增删改」的
  全部依据，一个来路不明的 id 会让后续删除打到不确定的那一门上。
  改成**拒绝**（丢弃该条），并靠迁移保证真实数据都有 id。
- **`weeks` 认不出来时回落到「每周」**：把「双周」猜成「每周」会让用户
  在错误的周次看到课，比直接不显示更难发现。改成拒绝。
- **空格子先显示一个空列表再点「添加」**：白加一步。空格子直接进表单。

**结果如何**
四条新断言全绿：`multiOk` / `filterOk` / `noSilentDeleteOk` +
`verifyTimetableMigration()`（形状、老课程仍在、`weeks` 为 `all`、补了 id、
幂等、备份拍在迁移之前）。
**验证方式**：`npm run typecheck`、`npm run smoke`、`npm run smoke:timetable`、
`npm run smoke:migrate`、`npm run smoke:i18n` 全通过。

**下一步依赖什么**
1. 用户审 `feat/week-rules` 后合并（**没有合并回 `master`**）
2. 打包 0.4.0（用户明确说本轮不做）
3. 实机验一次「0.3.0 装好录课 → 0.4.0 覆盖安装 → 课都在」——
   自动化只验到文件层面，这一步得人工确认

---

## D-011 · 日历做成「独立一页 + 独立存储」，课表事件按周次铺到日期上

**做了什么**
用户原话：「我还想给这个软件再集成一个日程功能（以日历为基础）」。
在 `feat/calendar` 上落了四件事：

- **独立一页**：新路由 `calendar` + 侧栏入口，`src/renderer/views/calendar-view.ts`
- **独立存储** `calendar.json`：`semesterStart`（开学第一天）+ `events[]`；
  日程字段 `id / title / date / start / end / location / note / repeat / remindBefore`
- **课表事件**：靠 `semesterStart` 把课表的「第几周第几列」换算成具体日期，
  再按 `WeekRule` 过滤（`@shared/weekRule` 的 `weeksInclude`）
- **自建日程**：重复规则只做 `once / weekly / monthly`；
  到点提醒走主进程 `Notification` + 窗口开着时另发一条应用内提醒作兜底

**为什么这么选**
- **独立一页，不并进课表页**：两者回答的是不同的问题 —— 课表 = 这学期每周
  怎么上；日历 = 某一天有什么。塞进同一页只会让两边都变窄。
- **独立文件，不塞进 `timetable.json` / `settings.json`**：日程与课表的
  生命周期不同（课表一学期一换，日程是长流水），混在一起会让两边都难迁移。
- **`DATA_SCHEMA` 保持 2**：这是纯新增文件，**没有改动任何既有存储的读写
  格式**，按 D-001 立的判据不构成一次 schema 变更。为了加一条日历就升 schema，
  会把所有老用户推上「备份 + 迁移」的重路径，换不到任何保护。
- **但必须在 `LEGACY_DATA_ENTRIES` 里登记 `calendar.json`**：`hasUserData()`
  靠这个清单判「全新安装」还是「老数据」。漏登记的话，一个**只记了日程、
  没有别的文件**的数据目录会被判成全新安装 —— 于是不备份就写印记，
  用户的老日程直接失去回头路。这个坑项目里踩过一次。
- **`weeksInclude` / `visibleCourses` 从 `timetable-panel` 搬到 `@shared/weekRule`**：
  日历要算「这一周有哪些课」，判据必须与课表**完全一致**。各写一套迟早会对
  「什么算单周」产生分歧，而且两边都不报错，只是显示的东西不一样。
  课表页的既有引用改成从 shared 引，行为不变（`smoke:timetable` 的
  `filterOk` 盯着这条）。
- **重复规则只做三种**：课表那套「单双周」是课表特有的表达（教务系统按周排课），
  日程用不上。「每年」等更复杂的规则等有人真的提再说。
- **没填开学日就只显示自建日程，课表事件一律不显示**：猜一个开学日猜错了，
  会把课铺到错误的星期上 —— 那比不显示更糟，用户不会发现。
- **提醒回执分 `delivered / failed / unverified` 三态**：理由见 D-012。
  另外给了一个「测试提醒」按钮，让用户在自己机器上当场确认。

**否掉了哪些方案**
- **并进课表页当一个 Tab**：两边都会变窄，且「今天有什么」和「这学期怎么排」
  的操作频率完全不同。
- **把日历事件塞进 `timetable.json` 或 `settings.json`**：见上。
- **渲染层直接用 Web Notification API**：`security.ts` 里对渲染层的通知权限
  是**一律拒绝**的，那条红线不动。改走主进程。
- **每月重复遇到没有 31 号的月份就顺延到下月 1 号**：会让「每月 31 号」在
  2 月变成 3 月 1 日，与「每月 1 号」的日程撞在同一天。改成**跳过那个月**。
- **补发应用没运行时错过的提醒**：补发一堆「三天前的课要上了」除了打扰
  没有别的用处。启动时 `fromMs` 就是启动时刻，之前的一律落在窗口外。
- **把 `calendar.json` 的 schema 也升一级**：见上，纯新增不动 schema。

**结果如何**
`npm run smoke:calendar` 全绿（首跑即过）。渲染层断言：
`navOk` / `semesterOk`（开学日回填）/
`week1AllOk` + `week1OddOk` + `week1EvenAbsentOk`（第 1 周：全周课在、单周课在、
双周课不在）/ `week4AllOk` + `week4EvenOk` + `oddAbsentInWeek4Ok`（第 4 周反向）/
`weeklyOk`（每周重复连续三周都在）/ `monthlyThisMonthOk` + `monthlyNextMonthOk` +
`monthlyExactOk`（跨月，且月底那两天不串）/ `onceOk` + `onceNotElsewhereOk` +
`onceInDayListOk`（界面新建的一次性日程只落在当天）/ `notifyOk` / `persistOk`。
主进程断言：`verifyCalendarLegacyRecognized()`（备份里找得到升级前那条日程，
证明 `hasUserData()` 认了新文件）、`verifyCalendarPersistence()`（磁盘一份 +
新 store 重读一份，且 `repeat` / `start` / `remindBefore` 都原样回来）、
`verifyCalendarReminders()`（到点判定 + 左开右闭不重复挑）。

> 断言全部落在**格子里的实际文字**上，不是「元素在不在」：日历最容易出的错是
> 「日期算错了」—— 课照样渲染、格子照样在，只是摆到了错误的星期上。
> 日期一律用写死的靶子（开学日 2026-09-07 是周一），不靠「今天」定位。

**下一步依赖什么**
1. 用户审 `feat/calendar` 后决定是否合并（**没有合并回 `master`**）
2. 打包（用户明确说本轮不做）
3. 通知那条见 D-012 —— 这是唯一一处「自动化验不到、需要用户在真机上确认」的能力

---

## D-012 · 通知能不能弹，取决于「这台机器上装过没有」，不是签名

**做了什么**
用独立探针实测了 Windows Toast 的连通性，而不是照文档推断。
**结论先写：未签名的安装版能正常弹；开发态 / 免安装直接跑 exe 弹不出来。
门槛是 AUMID 有没有被注册，跟签名无关。**

**怎么实测的（可复现）**
环境：Electron 44.3.0 / Windows 11 / **未签名**构建。
探针是一个最小 Electron app，只做三件事：按环境变量 `setAppUserModelId` →
`new Notification(...).show()` → 落盘 Electron 侧的 `show` / `failed` 回执。

**判据不看 `show` 事件，看注册表。** Windows 把一条 toast 收下时会在
`HKCU\Software\Microsoft\Windows\CurrentVersion\Notifications\Settings\<AUMID>`
下写 `LastNotificationAddedTime`；被静默丢掉的 toast **不会留下这个键**。
三组对照：

| 设置 | Electron 的 `show` | 注册表留下 `LastNotificationAddedTime` |
|---|---|---|
| 不设 AUMID | **发了** | 否 |
| 设成没注册过的 AUMID（`io.github.studyboard.probe3`） | **发了** | 否 |
| 设成安装包注册过的 AUMID（`io.github.studyboard.app`） | 发了 | **是**（2026-09-16 12:05:03） |

第三组跑的是 `node_modules` 里那个**没签名**的 `electron.exe`，AUMID 对上
安装包（NSIS）建的那条开始菜单快捷方式之后，通知就真的进了系统通知库。
反过来说，前两组的 `show` 都触发了 —— **`show` 事件不能当作「用户看到了」的证据**。

**为什么这么选**
- **代码里必须 `app.setAppUserModelId()`，且要与 `electron-builder.yml` 的
  `appId` 一致**（两边现在都是 `io.github.studyboard.app`）。不一致的话，
  安装版也会掉进第二行那种「发了但被丢掉」的形态。
- **回执分三态如实上报**：`delivered`（收到 `show`）/ `failed`（收到 `failed`
  或构造抛错）/ `unverified`（1.5 秒内既没 `show` 也没 `failed`）。
  `unverified` 就是上表前两行的形态，不能算成功。
- **给一个「测试提醒」按钮**：通知通不通还取决于用户那台机器的勿扰设置，
  程序猜不出来。与其让用户等一条可能永远不来的提醒，不如让他当场试一次。
- **窗口开着时另发一条应用内提醒兜底**：系统通知被拦掉时，用户不至于
  什么都不知道。

**否掉了哪些方案**
- **拿 `show` 事件当成功**：实测证明它会在通知根本没被系统收下时照样触发。
- **渲染层用 Web Notification API**：`security.ts` 对渲染层的通知权限一律拒绝。
- **给免安装版也补一条注册**（首次运行时写
  `HKCU\Software\Classes\AppUserModelId\<AUMID>`，这是未打包应用注册 AUMID 的
  官方机制）：**没做**。它要动用户注册表，属于得用户拍板的事。
  如果将来要支持「便携版也能弹提醒」，这就是那条路。
- **弹窗自己画一个提醒框**：不是系统通知，用户不在应用里时看不到，
  等于把「到点提醒」这个功能砍掉一半。

**结果如何**
`smoke:calendar` 里**不真的发通知**（自动化测试不该往用户桌面上弹东西），
只断言 `reminders.info()` 的形状合法、`supported` 是布尔值，以及用纯函数
`dueReminders` 把「到点判定 + 左开右闭」钉死。真正的投递只能在真机上人工确认 ——
`verifyCalendarReminders()` 的注释里写明了这一点。

**下一步依赖什么**
1. **用户在真机上点一次「测试提醒」**：装 0.4.0 之后确认到底弹不弹，
   以及弹出来的是不是「StudyBoard」这个身份（而不是 electron.app.Electron）。
   自动化到不了这一步。
2. 如果用户要求「免安装版也能弹」，再回头做上面那条 AUMID 注册（要用户同意动注册表）。
3. 代码里那张实测表原来有一行写错了（「不设 → `show` 不发」），
   实测是不设也会发。**已就地更正** —— 那张表是给人做判断用的，写错比不写更坏。
