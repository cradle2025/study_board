# DECISIONS.md — 决策记录

> **只追加，永不修改历史条目。**
> 发现旧条目写错了，追加一条新的说明更正，不要编辑旧的 ——
> 这个文件的价值在于「当时的判断依据」，改掉它就失去了这个价值。
>
> 条目格式：
> - **做了什么**
> - **为什么这么选**
> - **否掉了哪些方案（附理由）**
> - **结果如何（含验证方式）**
> - **下一步依赖什么**

---

## D-001 · 数据格式用「版本闸口」而不是靠各存储自己的 version 字段

**做了什么**
新增 `src/main/services/dataVersion.ts`，数据根目录放 `data-version.json`
记录 `schema` 与写入它的 app 版本。启动时（建存储之前）过闸口，四条分支：
`fresh` 写印记 / `same` 只刷新 app 版本 / `upgrade` 先整目录备份再逐级迁移 /
`downgrade`（数据比程序新）备份 + 显著提示**但继续启动**。

**为什么这么选**
各存储原来自己的 `version` 字段**只写不读**，等于注释 —— 没有任何代码
依据它做判断。数据格式变更时没有任何保护。

降级**不阻止启动**是有意的：直接拒绝启动对学习工具太重（用户打不开自己的
东西），静默继续又等于默认同意丢数据。折中是备份 + 提示。

**否掉了哪些方案**
- 「各存储自己管迁移」：迁移需要跨存储的一致性（比如课表和卡片同时变），
  分散管理会出现「一半迁完一半没迁」的状态。
- 「降级时拒绝启动」：太粗暴，且用户没有自救路径。

**结果如何**
`npm run smoke:update`（降级保护）与 `npm run smoke:migrate`（老数据升级）通过。
**验证方式**：两个场景对印记的期望正好相反（降级要保留、升级要改写），
所以必须分开断言 —— 第一版把两者并进一条共用函数，升级场景被误判失败。

**下一步依赖什么**
将来每次改数据格式，都要递增 `DATA_SCHEMA` + 注册迁移函数 + 加自检场景。

---

## D-002 · 便携模式改成「真的搬数据」

**做了什么**
新增 `settings:set-portable` 通道，真的复制数据、写 `portable.flag`、重启生效。
`portableMode` 从 `SettingsPatch` 移除，`sanitizePatch` 遇到它直接报错。

**为什么这么选**
原来开关只写了 `config.json` 的一个字段，而数据读哪里由
`detectPortableMode()`（看程序同级有没有 `portable.flag`）决定 ——
`writePortableFlag` 和 `migrateTo` 是**没有任何调用点的死代码**。
开关看着生效了（勾变了、还提示「重启后生效」），数据位置从来没动过。

**否掉了哪些方案**
- 「静默忽略未知字段」：**静默忽略正是原 bug 的形态**。
  宁可让 IPC 报错，也不要再出现「看起来生效了其实没动」。

**结果如何**
开关真的搬数据。**验证方式**：设置页操作后检查目标目录有文件、
`portable.flag` 存在、重启后应用读的是新位置。

**下一步依赖什么**
无。

---

## D-003 · 升级时用 NSIS 钩子保住便携数据（附一个跨版本契约）

**做了什么**
`build/installer.nsh` 里用 `customUnInstall` / `customInstall`，
在 `${isUpdated}` 时把 `study-board-data` 挪到 `$TEMP` 再挪回来。

**为什么这么选**
便携数据目录就在安装目录里，而 NSIS 卸载器会把 `$INSTDIR` 整个删掉
（重命名到 `$PLUGINSDIR\old-install` 再 `RMDir /r`，那是个会被清理的临时目录）。
升级时也会先跑旧版卸载器，所以升级同样会删。

**没有**接管默认删除逻辑（用 `customRemoveFiles`），因为模板里的
`un.atomicRMDir` 处理了「文件被占用」的情况，接管会丢掉那个能力。

**否掉了哪些方案**
- 「把便携数据放到安装目录之外」：便携模式的意义就是数据跟着 U 盘走，
  放到 `%LOCALAPPDATA%` 就失去意义了。

**结果如何**
实机验证：装 0.3.0 → 植入 `study-board-data`（含一篇内容可辨认的笔记）→
用同一个包覆盖安装 → **数据完好无损**，`$TEMP` 无暂存残留。

**⚠️ 一个必须记住的约束（实测得出，不是推断）**
装 0.1.0（无保护）→ 植入数据 → 装 0.3.0（有保护）→ **数据被删了**。
因为**升级时跑的是「已安装版本」的卸载器**，不是新版编译出来的那个。
所以这套保护只在「被升级的版本自己也带这段逻辑」时生效。

对初版无影响（0.1.0 / 0.2.0 从未对外发布），但：

**`SB_STASH_DIR` 是跨版本契约，不能改。**
升级时「暂存到哪」由旧版卸载器决定，「从哪恢复」由新版安装程序决定 ——
两边是两个不同版本编译出来的代码。改这个路径会让从旧版升级的数据
留在 `$TEMP` 而新版找不到，表现就是「升级后数据不见了」。

**下一步依赖什么**
任何改 `installer.nsh` 的人都要先读文件顶部的注释。

---

## D-004 · i18n 用扁平点分 key + 两层降级，词典放 `shared`

**做了什么**
`src/shared/i18n/` 放 `zh-CN.ts` / `en-US.ts` / `index.ts`，
`translate(lang, key, params)`；渲染层包一层 `t()` / `tm()`。

**为什么这么选**
- **放 `shared` 而不是渲染层**：主进程抛的报错也会出现在界面上
  （IPC 错误经 `formatError` 变成 toast），两边必须用同一份词典。
  放在任一侧都会让另一侧只能硬编码。
- **扁平点分 key 而不是嵌套对象**：`t('notes.group.rename')` 这种写法让
  「漏翻译」可以被机器查出来 —— 嵌套对象要递归比对，扁平表一句
  `Object.keys` 就能比出差异。自检里那条「中英词条一一对应」的断言
  就是靠这个成立的。
- **两层降级**：英文缺 → 回落中文（用户至少看得懂）；两边都缺 →
  显示 key 本身。后者看着丑，但那正是它的意义：界面上出现
  `notes.foo.bar` 比出现空字符串好排查得多。也正因为这个降级，
  翻译可以分批做，中途不会把界面弄坏。

**否掉了哪些方案**
- 「用成熟的 i18n 库（i18next 等）」：需求只有「两语言 + 参数插值 +
  缺翻译降级」，引入库带来的体积与配置复杂度不划算，且库的
  「缺翻译回落」行为不容易被自检断言盯住。

**结果如何**
602 词条，中英一一对应。**验证方式**：
`npm run smoke`（含 `dictionaryOk()` 断言）+ `npm run smoke:i18n`。
渲染层只剩 2 处 `console.*` 中文。

**下一步依赖什么**
加词条必须两边一起加；改完跑 `smoke:i18n`。

---

## D-005 · 主进程的中文报错不逐个改造，改在「展示那一刻」查表

**做了什么**
主进程继续 `throw new Error('中文')`；渲染层展示前用 `tm()` 查表翻译，
查不到就原样显示中文。

**为什么这么选**
主进程的中文报错散在几十个服务里，还有很多是**动态拼**的
（如 `资料最多 200 份`）。逐个改造成 key 的成本极高，而且改错就丢错误信息
（报错本身是调试线索）。在展示层查表是成本最低、风险最小的位置。

**否掉了哪些方案**
- 「主进程改成抛错误码，渲染层拼文案」：动几十个文件，
  且拼装过程本身会引入新的漏翻点。
- 「主进程也 import 词典并翻译」：主进程不知道渲染层当前的语言
  （语言是渲染层的模块级状态），要传语言进来又是一圈改动。

**结果如何**
英文界面下大部分主进程报错会显示英文，少数动态拼的仍显示中文
（有意的降级）。**验证方式**：`smoke:i18n` 的逐页截图里没有出现
突兀的中文报错。

**下一步依赖什么**
新增主进程报错时，如果是**固定文案**，在 `en-US.ts` 里加同 key 即可；
如果是动态拼的，暂时接受显示中文。

---

## D-006 · 切换语言用「重绘」而不是「逐个更新文案」

**做了什么**
语言是模块级状态；`AppShell.#relocalize()` 重建外壳 DOM、重挂导航、
再 `go()` 回当前路由。

**为什么这么选**
文案散布在几十个视图与组件的构造函数里，还有一部分是**拼在 HTML 字符串里**
的。逐处更新等于给每个视图再写一遍渲染逻辑，漏一处就是一处永远不变的中文。
重建只有一处代价（当前视图的临时状态会丢），而切语言本来就是低频动作。

**踩到的坑（重要）**
`go()` 里有一句「路由没变就直接返回」，所以重绘前必须把 `#currentRoute`
清掉 —— 不清的话重挂载会静默什么都不做，表现是「切了语言但界面纹丝不动」，
而且**不报任何错**。

**否掉了哪些方案**
- 「逐层传 `lang` 参数」：会污染几乎每个函数签名，收益只是
  「同一时刻能存在两种语言的界面」——这个应用不需要那个能力。

**结果如何**
`npm run smoke` 的 `languageSwitchOk: true`（看板 → Dashboard → 看板，**双向**都验）。

**下一步依赖什么**
新增视图不需要各自订阅语言变化 —— 外壳重建会带上它们。
但如果某个视图在**模块加载期**就把文案固化进常量（比如 `Record<X, string>` 存文案），
那它永远变不了。这类表必须存 key。

---

## D-007 · 课表「连堂复用」：做两条路径，不做整列复制

**做了什么**
1. 单元格编辑器加「复制上一节」按钮（同一天上一节，**整格复制**）
2. 加「复用已录入的课程」下拉 + 课程名输入框的原生 `list` autocomplete
   （只填课程身份：名称/老师/地点/时长，**不填备注**）

**为什么这么选**
用户的原话是「很多课程是连堂上的」—— 连堂在表格里就是上下相邻的格子。
「复制上一节」一下点完，是最直接的解。
下拉覆盖非相邻的情况（周三的课和周五的课是同一门）。
不填备注是因为备注往往是**这一格特有的**（「小测」「调课」），
自动填进去反而要用户删。

**否掉了哪些方案**
- **「整列复制」**（把周一整列复制到周三）：**用户明确否决，理由是不值得**。
  我评估后同意：它有个确认框，说明是危险操作，而如果一周七天本来就不一样，
  这按钮根本用不上。技术上它也缺前提（见下）。

**结果如何**
`npm run smoke:timetable` 的 `reuseOk: true`、`reuseOptionCount: 5`、
`nameAfterCopy: "编译原理"`。

**下一步依赖什么**
无。但**顺带查清了一件事**：面板只有 `onSaveCell(key, cell)`，
而 `TimetableStore` 每个单格方法都调一次 `#persist()`（整份 JSON 重写）。
所以任何**多格操作**都必须先加批量接口（`setCells`），
否则 = N 次整文件重写 + 中途失败留下半成品。这个结论在周次功能里用得上。

---

## D-008 · 周次功能选「单元格级周次」，不做「多套课表」

**做了什么**
（决策已定，实现待做 —— 见 `HANDOFF.md` 第 8 节）

`TimetableCell` 加 `id` + `weeks: WeekRule`；
`cells` 从 `Record<key, TimetableCell>` 变成 `Record<key, TimetableCell[]>`。

**为什么这么选**
用户明确选了三个方案里最灵活的那个。而且它**最贴近真实教务系统**：
真实的课表就是「某个时间段、某些周、上某门课」，同一时间段不同周可以
是不同的课。用「多套课表」表达这个需要把同一门课在几套表里重复录入。

**否掉了哪些方案**
- **「单双周」**（只分两套）：最省事，但用户说「每一周都可能不一样」，
  单双周表达不了「前八周一门、后八周另一门」。
- **「多套课表按周次绑定」**（A/B/C 表各绑周次范围）：比单双周灵活，
  但同一门课要在多套表里重复录；且「第 3 周该显示哪套」这个映射本身
  就是个额外概念，用户要学。

**结果如何**
未实现，无法验证。

**下一步依赖什么**
⚠️ **数据层与 UI 层必须同一次落地。** 数据层先支持「一格多门课」、
UI 还按「一格一门课」读写的话，用户双击编辑再保存会**静默删掉**
这一格里其它周次的课。所以没有安全的中间点，不能分阶段。

---

## D-009 · 发布走 GitHub Releases，不做代码签名（用户决策）

**做了什么**
`.github/workflows/release.yml`：推 `v*` tag 触发，构建 Windows 安装包 +
`.blockmap` + `SHA256SUMS.txt`，先出 **draft** Release。

**为什么这么选**
用户明确说「走 github 发布，不走软件商店之类的」。
这个前提下，**签名从「必需品」降级成「可选项」**：
- 发布机制本身不需要签名（GitHub Releases 就是文件下载）
- 但 **SmartScreen 照样会拦** —— 它看的是「有没有可信签名 + 下载量够不够」，
  跟从哪下载无关。用户第一次运行仍会看到「已保护你的电脑」

**否掉了哪些方案**
- **买 OV / EV 证书**（约 $200 / $400 每年）：用户尚未决定。
  不签也能发，只是每个用户过一次拦截框。已把「不签名」这件事写进
  README 与 Release 正文，免得用户看到拦截框以为中招了。

**顺带查证（省得以后再翻）**
`electron-builder` 的 GitHub provider **不会**从 `GITHUB_REPOSITORY`
环境变量推断仓库 —— 它读的是 `package.json` 的 `repository` 字段
（`app-builder-lib/out/publish/PublishManager.js` 的 `packager.info.repositoryInfo`）。
但现有工作流用的是 `--publish never` + `softprops/action-gh-release` 挂附件，
**两条都不需要**，所以不受影响。

**结果如何**
工作流已就位，**从未在真实 Actions 上跑过**（仓库还没建）。

**下一步依赖什么**
1. 用户建 GitHub 仓库（指导已发出）
2. 确认用户的 GitHub **登录名**（显示名是 `摇篮2025`，但登录名是 ASCII）
3. 之后才能补 `repository` / `homepage` / `bugs` 字段
